<local:OneViewBase x:Class="MVVMFirma.Views.NewPurchaseContractItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Item Name"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding Name}"/>

        <Label Grid.Row="1" Content="Category"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,5"
                  ItemsSource="{Binding CategoriesItems}"
                  DisplayMemberPath="name"
                  SelectedValuePath="category_id"
                  SelectedValue="{Binding Category_Id}"/>

        <Label Grid.Row="2" Content="Condition"/>
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,5"
                  ItemsSource="{Binding ItemConditionsItems}"
                  DisplayMemberPath="name"
                  SelectedValuePath="condition_id"
                  SelectedValue="{Binding Condition_Id}"/>

        <Label Grid.Row="3" Content="Description"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,5" Text="{Binding Description}" Height="50" TextWrapping="Wrap"/>

        <Label Grid.Row="4" Content="Sale Price (Shop)"/>
        <TextBox Grid.Row="4" Grid.Column="1" Margin="0,5" Text="{Binding Sale_Price, Mode=TwoWay, ValidatesOnDataErrors=True}"/>

        <Label Grid.Row="5" Content="Purchase Price" FontWeight="Bold"/>
        <TextBox Grid.Row="5" Grid.Column="1" Margin="0,5" FontWeight="Bold" 
                 Text="{Binding Estimated_Value, Mode=TwoWay, ValidatesOnDataErrors=True}"/>

        <Label Grid.Row="6" Content="Branch"/>
        <ComboBox Grid.Row="6" Grid.Column="1" Margin="0,5"
                  ItemsSource="{Binding BranchesItems}"
                  DisplayMemberPath="name"
                  SelectedValuePath="branch_id"
                  SelectedValue="{Binding Current_Branch_Id}"/>
    </Grid>
</local:OneViewBase>