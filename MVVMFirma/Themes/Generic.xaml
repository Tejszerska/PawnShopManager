<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:MVVMFirma.Views">

    <Style TargetType="{x:Type local:AllViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:AllViewBase}">
                    <Grid Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="34"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/> <!--* oznacza pozostały obszar-->
                        </Grid.RowDefinitions>
                        <ToolBar Grid.Row="0" Height="30" Margin="0,2,0,2">
                            <ToggleButton Content="Add" Height="30" Width="70" Command="{Binding AddCommand}" Foreground="{DynamicResource MainText}"/>
                            <ToggleButton Content="Edit" Height="30" Width="70" Foreground="{DynamicResource MainText}"/>
                            <ToggleButton Content="Delete" Height="30" Width="70" Foreground="{DynamicResource MainText}"/>
                            <ToggleButton Content="Refresh" Height="30" Width="70" Command="{Binding LoadCommand}" Foreground="{DynamicResource MainText}"/>
                        </ToolBar>
                        <Expander Grid.Row="1"  Header="Filter and Sort"  Foreground="{DynamicResource MainText}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition  Height="25"/>
                                        <RowDefinition  Height="25"/>
                                        <RowDefinition  Height="30"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition  Width="230"/>
                                        <ColumnDefinition  Width="60"/>
                                        <ColumnDefinition  Width="230"/>
                                    </Grid.ColumnDefinitions>
                                    <Label  Grid.Row="0"  Grid.Column="0" Content="Sort:  "/>
                                    <ComboBox  Grid.Row="1"  Grid.Column="0" ItemsSource="{Binding  SortComboboxItems}"  SelectedValue="{Binding Path=SortField,Mode=TwoWay}"  Margin="0,0,60,0"/>
                                    <Button  Content="Sortuj"  Grid.Row="1"  Grid.Column="0" Command="{Binding  SortCommand}" Margin="170,0,0,0"/>
                                    <Label  Grid.Row="0"  Grid.Column="2" Content="Search:  "/>
                                    <ComboBox  Grid.Row="1"  Grid.Column="2" ItemsSource="{Binding  SearchComboboxItems}"  SelectedValue="{Binding Path=SearchField,Mode=TwoWay}"  />
                                <TextBox  Grid.Row="2"  Grid.Column="2" Margin="0,5,60,0"  Text="{Binding  SearchTextBox}"/>
                                <Button  Content="Szukaj"  Grid.Row="3"  Grid.Column="2" Command="{Binding  SearchCommand}" Margin="170,5,0,0"/>
                                </Grid>
                        </Expander>
                        <ContentPresenter Grid.Row="2" Margin="0,5,0,5" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:OneViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:OneViewBase}">
                    <Grid Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="34"/>
                            <RowDefinition Height="*"/>                            
                        </Grid.RowDefinitions>
                        <ToolBar Grid.Row="0" Height="30" Margin="0,2,0,2">
                            <ToggleButton Content="Save and close" Command="{Binding SaveAndCloseCommand}" Foreground="{DynamicResource MainText}"/>
                            <ToggleButton Content="Cancel" Foreground="{DynamicResource MainText}"/>
                        </ToolBar>
                        <ContentPresenter Grid.Row="1" Margin="0,5,0,5" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
