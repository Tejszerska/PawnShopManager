<local:OneViewBase x:Class="MVVMFirma.Views.NewPayment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        
        <Label Grid.Column="0" Grid.Row="0" Content="Payment date" Margin="0,8,0,8"/>        
        <DatePicker Grid.Row="0" Grid.Column="1" Margin="0,5"
            Height="28" 
            VerticalContentAlignment="Center"
            SelectedDate="{Binding Payment_Date}"/>
        
        <Label Grid.Column="0" Grid.Row="1" Content="Amount" Margin="0,8,0,8"/>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="0,8,0,8" Text="{Binding Path=Amount, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Column="0" Grid.Row="2" Content="Payment method ID" Margin="0,8,0,8"/>
        <ComboBox Grid.Row="2" Grid.Column="1" Height="25" 
  ItemsSource="{Binding PaymentMethodsItems}"
  DisplayMemberPath="name"
  SelectedValuePath="payment_method_id"
  SelectedValue="{Binding Payment_Method_Id, Mode=TwoWay}"
  VerticalContentAlignment="Center"/>

        <Label Grid.Column="0" Grid.Row="3" Content="Description" Margin="0,8,0,8"/>
        <TextBox Grid.Column="1" Grid.Row="3" Margin="0,8,0,8" Text="{Binding Path=Description, UpdateSourceTrigger=PropertyChanged}"/>

    </Grid>
</local:OneViewBase>
